+++
title = "What's on the Bench"
date = 2026-07-20T11:00:00+03:00
draft = false
tags = ["electronics", "meta"]
summary = "A first post, mostly to confirm that images render the way I expect."
cover = "/images/sample-bench.jpg"
+++

This post exists to prove image rendering works, using Hugo's **built-in**
`figure` shortcode — never a theme-specific one, so the content stays
portable across themes.

{{< figure src="/images/sample-bench.jpg" alt="The workbench" caption="The bench, mid-project as usual." >}}

Inline code renders like `dmesg | tail`, and blocks get highlighted:

```bash
sudo dmesg --follow
```
